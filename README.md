# class-sorting-algorithm

C++で作成したクラス分けアルゴリズム

それぞれのクラスには 1人以上の生徒を配置，Sの生徒の数とAの生徒の数の差を最小にする。
そのような分け方が複数考えられる場合は，その中でSクラスの人数が最小となるような分け方にする。

仕組み
1. 生徒の名前と点数を入力
2. 点数順に並べ替える
3. クラスの人数差が最小になる境界を探す
4. 同点の途中では分けない
5. Aクラス / Sクラスに自動分類
6. CSVファイルに出力

計算量
時間計算量：O(n log n)
